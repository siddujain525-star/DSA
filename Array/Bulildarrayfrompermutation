class Solution {
    public int[] buildArray(int[] nums) {
        int n = numms.length;
        int [] ans = new int[n];
        for(int i = 0 ; i < numms.length ; i++ ){
            ans[i] = nums[nums[i]];
        }
        return ans;
        
    }
}
